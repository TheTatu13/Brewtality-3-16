name: Tests

# CANONICAL CALLER PATTERN — copy this whole file into a new company
# scraper repo unchanged. See scrape.yml for why the actual logic lives in
# tests-reusable.yml instead of here.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  tests:
    uses: TheTatu13/Brewtality-3-16/.github/workflows/tests-reusable.yml@v1
